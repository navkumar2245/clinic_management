<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Prescription Pad</title>
  <style>
    body, html {
      margin: 0;
      padding: 0;
      height: 100%;
    }
    .prescription-pad {
      height: 100%;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      background-color: #f5f5f5;
    }
    .canvas-container {
      border: 2px solid #333;
      max-width: 80%;
      max-height: 80%;
      overflow: auto;
    }
    canvas {
      display: block;
      margin: 0 auto;
    }
    .save-button {
      margin-top: 20px;
      padding: 10px 20px;
      background-color: #007bff;
      color: #fff;
      border: none;
      cursor: pointer;
    }
  </style>
</head>
<body>
  <div class="prescription-pad">
    <div class="canvas-container">
      <canvas id="prescriptionCanvas"></canvas>
    </div>
    <button class="save-button" onclick="savePrescription()">Save Prescription</button>
  </div>

  <script src="https://cdnjs.cloudflare.com/ajax/libs/fabric.js/4.5.0/fabric.min.js"></script>
  <script>
    var canvas = new fabric.Canvas('prescriptionCanvas');

    function savePrescription() {
      var dataUrl = canvas.toDataURL('image/png');

      // Send dataUrl to server via AJAX or form submission
    }
  </script>
</body>
</html>
